# azure-devops-ui 2.277 still declares React 16 peers, although the viewer is
# validated against React 18. Keep clean installs reproducible until its peer
# metadata catches up or the local UI boundary replaces it.
legacy-peer-deps=true
